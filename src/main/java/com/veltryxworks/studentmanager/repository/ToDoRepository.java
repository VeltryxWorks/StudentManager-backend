package com.veltryxworks.studentmanager.repository;

import org.springframework.data.jpa.repository.JpaRepository;

/**
 * extends JpaRepository<ToDo, Long>
 * Usi Spring Data JPA: basta estendere JpaRepository e ottieni gratis CRUD.
 *
 * Non scrivi SQL a mano (a meno che non servano query particolari).
 */
public interface ToDoRepository {
}
